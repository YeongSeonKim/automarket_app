<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.automarket_app">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 타이틀바 없애기 -->
    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/app_icon_128"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".OrderPopupActivity"/>

        <service
            android:name=".service.ProductService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".CartActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".CashActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />


        <meta-data
            android:name="api_url"
            android:value="http://192.168.0.16:8081/automarket"/>
      <!--  <meta-data
            android:name="api_url"
            android:value="http://70.12.115.56:8080/automarket"/>-->
      <!--<meta-data
            android:name="api_url"
            android:value="http://70.12.115.69:8082/automarket" />-->
        <meta-data
            android:name="api_key"
            android:value="" />
    </application>

</manifest>